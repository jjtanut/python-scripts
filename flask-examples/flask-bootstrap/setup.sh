# Some misc scripts to setup (built for python 3)

# first install pip for python 3
# on ubuntu: apt-get install python3-pip

# install virtualenv: pip3 install virtualenv

# set up virtualenv: virtualenv -p python3 ENV

# install requirements
# pip3 install -r requirements.txt
